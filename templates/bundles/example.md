# Bundle: Example Domain

<!--
  BUNDLE TEMPLATE

  A bundle is a self-contained context file for one domain.
  Guidelines:
  - Keep under 180 lines (ideally 40-60)
  - Include everything Claude needs for this domain
  - No dependencies on other bundles (self-contained)
  - Use tables and lists for density
  - Include key commands, file paths, conventions
  - Skip anything Claude can infer from code
-->

## What This Covers
Brief description of what domain this bundle provides context for.

## Key Files
| File | Purpose |
|------|---------|
| `src/example.ts` | Main entry point |
| `config/example.json` | Configuration |

## Conventions
- Convention 1: description
- Convention 2: description

## Commands
```bash
# Build
npm run build

# Test
npm test

# Deploy
npm run deploy
```

## Architecture Decisions
- Decision 1: why this approach was chosen
- Decision 2: why this approach was chosen

## Common Patterns
- Pattern 1: when to use, how it works
- Pattern 2: when to use, how it works

## Gotchas
- Gotcha 1: non-obvious behavior to watch for
- Gotcha 2: common mistake and how to avoid
