---
description: Load jit-knowledge and the repo AGENTS.md bootstrap before substantive work.
globs:
alwaysApply: true
---

<!-- jit-knowledge-managed: cursor-rule-v1 -->

# jit-knowledge Load Rule

## Pull Latest First (required, every session)

Two or more developers work across several machines against the same repos
-- never assume the local clone is current. The BINDING per-session
requirement is scoped to THIS repo (your lane's working repo):

```
git -C /Users/danstolts/Code/jitneuro pull --ff-only
```

If refused (dirty tree or diverged branch), SURFACE it and resolve
(commit/stash/rebase) before mutating anything in this repo.

Knowledge/tooling currency (jit-knowledge, HubCentral) is separate and
NON-DISRUPTIVE by design. Cursor has no SessionStart hook, so run the same
two pulls best-effort at bootstrap:

```
git -C /Users/danstolts/Code/jit-knowledge pull --ff-only
git -C /Users/danstolts/Code/HubCentral pull --ff-only
```

These only fast-forward `main` (which advances solely via CI-gated merged
PRs), so they can never disturb in-flight branch/worktree work. If one is
refused because the clone is dirty, that lane is mid-work on this machine:
LEAVE IT ALONE, continue with the stale warning, and let that lane resolve
its own clone. Full rule:
`/Users/danstolts/Code/jit-knowledge\rules\master-agent-operational-requirements.md`
("Current repos").

This repo uses `AGENTS.md` as the canonical, tool-agnostic agent instruction
surface. At the start of substantive work, read:

1. `/Users/danstolts/Code/jitneuro\AGENTS.md` when present
2. `/Users/danstolts/Code\AGENTS.md` when present
3. `/Users/danstolts/Code/jit-knowledge\AGENTS.md`
4. `/Users/danstolts/Code/jit-knowledge\COMPANY.md`
5. `/Users/danstolts/Code/jit-knowledge\README.md`
6. `/Users/danstolts/Code/jit-knowledge\INDEX.md`
7. The active hub's `agent-inbox.md`, `agent-goals.md`, `agent-permissions.md`,
   `questions.md`, and `Hub.md` when present
8. `.agents\context.md` when present

Resolve `<KnowledgeRoot>` in this order:

1. `/Users/danstolts/Code/jit-knowledge`
2. `<repo>\.jit-knowledge`
3. `JIT_KNOWLEDGE_ROOT`
4. `~/.claude/workspace.json`
5. `%USERPROFILE%\Code\jit-knowledge`

If the knowledge root cannot be resolved, warn once and continue with repo-local
context only.

Cursor does not run Claude Code hooks or slash commands. Treat this rule and the
repo `AGENTS.md` as Cursor's bootstrap surface.

When the user names a jit-knowledge skill, rule, playbook, workflow, pattern, or
charter, find it in `/Users/danstolts/Code/jit-knowledge\INDEX.md`, read the referenced artifact,
and follow its instructions.

After bootstrap, apply `/Users/danstolts/Code/jit-knowledge\rules\agent-goals.md`,
`/Users/danstolts/Code/jit-knowledge\rules\multi-agent-repo-coordination.md`, and
`/Users/danstolts/Code/jit-knowledge\rules\session-working-tree-context.md`, and
`/Users/danstolts/Code/jit-knowledge\rules\autonomous-execution.md`, and
`/Users/danstolts/Code/jit-knowledge\rules\session-guardrail.md`, and
`/Users/danstolts/Code/jit-knowledge\rules\hubcentral-lane-registration.md`. Cursor has no
session hooks, so register this session's HubCentral lane (the stable repo, not
the branch) at start and poll inbox/ready/blocked at task boundaries by running
the shared scripts explicitly: `bash ~/.claude/hooks/session-start-lane-register.sh`
at start and `bash ~/.claude/hooks/intercom-agent-poll.sh` at task boundaries.
For cross-lane dependencies, run
`~/Code/HubCentral/scripts/cross-lane-handoff-curl.sh` to create the receiving
lane's `Ready` ticket and send Intercom with its ID. Never leave either write to
the Owner or receiving session.
Verify the current working
directory against the active hub/session state's working-tree context before
mutating files or dispatching workers. Read the active hub's
`agent-inbox.md`, `agent-goals.md`, and `agent-permissions.md` files. Cursor does not run Claude
`/goal`, `/loop`, or `/afk`; emulate the same contract by maintaining the active
plan, selecting the next READY approved task, and continuing until the repo or
repo-group lane backlog is clear or blocked.

Owner questions go in the active hub's `questions.md` file, preserving the
repo's existing hub directory case (`.hub\questions.md` or
`.HUB\questions.md`). When a question is added, open that file in the current
window when Cursor supports it.

Do not edit `.jit-knowledge/` from a consuming repo unless the user explicitly
asks to work in the submodule. Canonical knowledge changes belong in
`dstolts/jit-knowledge`.

## Mechanical contracts (hook parity)

Claude Code enforces these via hooks; Cursor has none, so apply manually,
every session:

1. **Ticket sweep before create.** Before any `devops-task create`, list
   New/Ready/Active/Blocked in the target lane(s) and grep titles
   case-insensitively for your keywords; any hit means read that ticket
   first. Include `SWEEP_EVIDENCE="lanes=... kw=... found=#id(disposition)..."`
   with the create. Fully covered -> extend the existing ticket, do not
   create; related -> create then `link-dep --dep-type blocks`. A remembered
   ticket is not a sweep.
2. **Audit existing surfaces.** Never plan or ticket a new page, form,
   module, or endpoint without inventorying overlapping surfaces and reading
   their bodies (a grep/index hit is not understanding). Extend or merge the
   best of each; never parallel-create. Canonical rule pending #4339.
3. **Planning input contracts.** User stories/outcomes with a prior-art
   inventory are required before any contract is valid. State effort in
   machine-hours with parallelizability and critical path, never
   days/weeks. Canonical rule pending #4339.
4. **Lane discipline.** Resolve your lane ONLY from your own session
   surfaces (git toplevel, heartbeat, session env). Never poll or resolve
   another lane's intercom inbox, even when a tool or stale env suggests it.
   `/Users/danstolts/Code/jit-knowledge\rules\multi-agent-repo-coordination.md`.
5. **Education deliverables.** A feature is not Resolved without its
   user-education deliverables (in-product help, quickstart) per
   `/Users/danstolts/Code/jit-knowledge\rules\definition-of-done.md` condition 2.
